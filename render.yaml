services:
  - type: static
    name: dycomms-staging
    env: static
    buildCommand: bun install && bun run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
